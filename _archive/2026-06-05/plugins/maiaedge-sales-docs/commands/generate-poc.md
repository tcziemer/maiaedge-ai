---
name: generate-poc
description: Generate a MaiaEdge Proof of Concept Agreement
---

Use the `sales-docs` skill to generate a POC Agreement. Preferred approach: use `scripts/build_poc.py` with JSON input and `templates/poc_agreement_template.docx`. See `poc_specs.md` for the JSON schema. Output as PDF + DOCX.
