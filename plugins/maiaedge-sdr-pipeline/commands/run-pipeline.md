---
name: run-pipeline
description: Run the full SDR pipeline — takes a list of companies + contacts and produces Smartlead-ready XLSX with 3-email sequences + LinkedIn messages
---

Run the `sdr-pipeline` skill with the user's input list. Process each contact through the full pipeline: HubSpot pull → Activity gate → Web research → Apollo enrichment → Segment verification → Angle selection → Email writing → Output generation.
