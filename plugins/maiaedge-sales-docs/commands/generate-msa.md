---
name: generate-msa
description: Generate a MaiaEdge Master Subscription Agreement
---

Use the `sales-docs` skill to generate an MSA. Load `msa_template.docx` from `templates/` and follow `msa_specs.md` for variable field locations. Output as PDF + DOCX.
