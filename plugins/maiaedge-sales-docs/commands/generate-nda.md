---
name: generate-nda
description: Generate a MaiaEdge Mutual Non-Disclosure Agreement
---

Use the `sales-docs` skill to generate an NDA. Preferred approach: use `scripts/build_nda.py` with JSON input and `templates/nda_template.docx`. See `nda_specs.md` for the JSON schema. Output as PDF + DOCX.
